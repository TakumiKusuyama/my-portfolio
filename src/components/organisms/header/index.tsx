import HeaderFeature from '../../../pages/features/header/create';

const HeaderOrganism = () => {
    // 今はロジック不要だが、将来ロゴやメニューの動きなどを入れやすくなる！
    return <HeaderFeature />;
};

export default HeaderOrganism;
